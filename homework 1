import java.util.Scanner;
class Main {
  public static void main(String[] args) {
    

     //Create a program that prints your name to the screen. The program output should resemble the following:

   /* Scanner sc = new Scanner(System.in);
    
    System.out.println("Enter name: ");
    String name = sc.nextLine();
    System.out.println(name); */

    //second task:

   /* System.out.println("Hello world!");
    System.out.println("(and all the people in the world)");*/

    //Create a program that counts how many seconds there are in a year. You can assume that a year consists of 365 days (therefore the year is not a leap year). The program should print the following, X should be replaced with the calculation of your program:

   /* int x = 365 * 60 * 60 * 24;
   System.out.println(x); */


    //Create a program that asks the user for two integers and then prints their sum. 

   /* Scanner sc = new Scanner(System.in);
    int x,y, sum;
    System.out.println("Enter first num: ");
    x = sc.nextInt();
    System.out.println("Enter second num: ");
    y = sc.nextInt();
    sum = x+y;
    System.out.println("The sum of two numbers is" + sum);

    */

    //Create a program that asks the user for a number and tells if the number is positive (i.e. greater than zero).
   /* Scanner sc = new Scanner(System.in);
    int x;
    System.out.println("Enter num: ");
    x = sc.nextInt();
   
    if(x >0){
      System.out.println("POSITIVE");
    }
    else{
      System.out.println("NEGATIVE");
    } */

    // Create a program that asks the user for two numbers and prints the greater of those two. The program should also handle the case in which the two numbers are equal
    /*Scanner sc = new Scanner(System.in);
    int x,y;
    System.out.println("Enter first num: ");
    x = sc.nextInt();
    System.out.println("Enter second num: ");
    y = sc.nextInt();

    if(x>y){
      System.out.println(x + " " + "is greater");
      
    }
    else if( x == y){
      System.out.println("The numbers are equal!");
    }
    else{
      System.out.println(y + " " + "is greater");
    } */
  }      
}
